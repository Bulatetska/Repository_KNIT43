<Window x:Class="lab9.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab9"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        Title="lab9" Height="400" Width="600">
    <Grid VerticalAlignment="Center" MaxWidth="400" Margin="20,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="16">Введіть своє ім'я:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Padding="2" FontSize="16" Text="{Binding Name}"></TextBox>
        <Button Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="4" Padding="8 4" FontSize="16" Click="Submit">Підтвердити</Button>
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Text="{Binding Message}"></TextBlock>
    </Grid>
</Window>
