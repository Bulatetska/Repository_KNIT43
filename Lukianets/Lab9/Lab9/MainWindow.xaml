<Window x:Class="WpfAppGreeting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Привітання" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введіть своє ім'я:" FontSize="16"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" FontSize="16" Margin="0,5,0,5"/>
        <Button Grid.Row="2" Content="Підтвердити" FontSize="16" Click="ConfirmButton_Click" Width="120"/>
        <TextBlock Grid.Row="3" Text="{Binding Greeting}" FontSize="16" Margin="0,10,0,0"/>
    </Grid>
</Window>
