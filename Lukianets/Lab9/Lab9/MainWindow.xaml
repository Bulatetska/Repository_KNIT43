<Window x:Class="WpfAppGreeting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Привітання" Height="400" Width="500" MinWidth="300" MinHeight="300">

    <!-- Viewbox масштабуватиме весь інтерфейс -->
    <Viewbox Stretch="Uniform">
        <Grid Width="400" Height="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Інструкція -->
            <TextBlock Grid.Row="0" Text="Введіть своє ім'я:" 
                       FontSize="18" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       TextWrapping="Wrap"/>

            <!-- Поле вводу -->
            <TextBox Grid.Row="1" x:Name="NameTextBox" FontSize="16" 
                     HorizontalAlignment="Stretch" Margin="0,10,0,10"/>

            <!-- Кнопка -->
            <Button Grid.Row="2" Content="Підтвердити" FontSize="16" Width="150"
                    HorizontalAlignment="Center" Click="ConfirmButton_Click"/>

            <!-- Привітання -->
            <TextBlock Grid.Row="3" Text="{Binding Greeting}" FontSize="18" 
                       HorizontalAlignment="Center" VerticalAlignment="Top" 
                       TextWrapping="Wrap"/>
        </Grid>
    </Viewbox>
</Window>
