<Window x:Class="_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="20">
        <Label Content="Введіть рядок для обробки:" FontWeight="SemiBold"/>
        <TextBox x:Name="InputTextBox" 
                 Height="30" 
                 Margin="0,5,0,15" 
                 Padding="5"
                 TextWrapping="Wrap"/>

        <Button Content="Обробити" 
                x:Name="ProcessButton"
                Click="ProcessButton_Click" 
                Padding="10" 
                Margin="0,0,0,25"
                Background="#007ACC"
                Foreground="White"
                FontWeight="Bold"/>

        <Label Content="Результат обробки:" FontWeight="SemiBold"/>

        <TextBlock x:Name="ResultTextBlock" 
                   Text="Тут буде виведено результат..." 
                   Margin="0,5,0,0"
                   Padding="10"
                   FontWeight="Bold"
                   Foreground="#333333"
                   Background="#F0F0F0"
                   TextWrapping="Wrap"/>
    </StackPanel>
</Window>
