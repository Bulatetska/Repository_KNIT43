<UserControl x:Class="Hospital.WPF.Views.HospitalOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Інформація про лікарню -->
        <GroupBox Grid.Row="0" Header="Інформація про лікарню" Margin="10" FontSize="14">
            <StackPanel Margin="15">
                <TextBlock Text="{Binding HospitalName}" FontSize="18" FontWeight="Bold" Foreground="#2c3e50"/>
                <TextBlock Text="{Binding Address}" Margin="0,8,0,0" FontSize="14"/>
                <TextBlock Text="{Binding Phone}" Margin="0,4,0,0" FontSize="14"/>
                <TextBlock Text="⏰ Години роботи: 08:00 - 20:00 (Пн-Пт), 09:00 - 18:00 (Сб)" 
                           Margin="0,8,0,0" FontSize="13" Foreground="#7f8c8d"/>
            </StackPanel>
        </GroupBox>
        
        <!-- Статистика -->
        <GroupBox Grid.Row="1" Header="Статистика" Margin="10" FontSize="14">
            <UniformGrid Columns="4" Margin="15">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="👨‍⚕️ Персонал" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalStaff}" FontSize="24" FontWeight="Bold" 
                               Foreground="#3498db" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="👥 Пацієнти" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalPatients}" FontSize="24" FontWeight="Bold" 
                               Foreground="#e74c3c" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="🏢 Відділення" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalDepartments}" FontSize="24" FontWeight="Bold" 
                               Foreground="#2ecc71" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="📅 Сьогодні прийомів" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TodaysAppointments}" FontSize="24" FontWeight="Bold" 
                               Foreground="#f39c12" HorizontalAlignment="Center"/>
                </StackPanel>
            </UniformGrid>
        </GroupBox>

        <!-- Додаткова інформація -->
        <GroupBox Grid.Row="2" Header="Про систему" Margin="10" FontSize="14">
            <StackPanel Margin="15">
                <TextBlock Text="Система управління лікарнею" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="• Управління пацієнтами та лікарями" FontSize="13" Margin="0,2,0,0"/>
                <TextBlock Text="• Запис на прийом до лікаря" FontSize="13" Margin="0,2,0,0"/>
                <TextBlock Text="• Ведення медичних записів" FontSize="13" Margin="0,2,0,0"/>
                <TextBlock Text="• Статистика та звітність" FontSize="13" Margin="0,2,0,0"/>
                <TextBlock Text="🚀 Версія 1.0 | .NET 9.0 | WPF MVVM" 
                           FontSize="12" Margin="0,15,0,0" Foreground="#7f8c8d" FontStyle="Italic"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
