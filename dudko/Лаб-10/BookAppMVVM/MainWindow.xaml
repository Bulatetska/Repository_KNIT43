<Window x:Class="BookAppMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Book Manager" Height="400" Width="600">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Books}"
                 SelectedItem="{Binding SelectedBook}"
                 DisplayMemberPath="Title"
                 Margin="5"/>

        <StackPanel Grid.Column="1" Margin="15">
            <TextBlock Text="Title"/>
            <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Author" Margin="0 5 0 0"/>
            <TextBox Text="{Binding NewAuthor, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Year" Margin="0 5 0 0"/>
            <TextBox Text="{Binding NewYear, UpdateSourceTrigger=PropertyChanged}" />

            <Button Content="Add Book" Command="{Binding AddBookCommand}" Margin="0 10 0 0"/>
            <Button Content="Delete Book" Command="{Binding DeleteBookCommand}" />
        </StackPanel>
    </Grid>
</Window>
